---
scope: Trading DSS v1 — cỗ máy ép kỷ luật FX & Crypto, solo tool, Next.js/Vercel; pipeline quyết định 4 tầng + backtest + LLM narrator + audit log
purpose: build-substrate + solution-design doc
altitude: feature (giữ epics của v1 nhất quán)
updated: 2026-07-04T08:19
---

- (event) Create intent, Coaching path. Đầu ra: spine + solution-design doc đầy đủ. Ngôn ngữ: Vietnamese.
- (constraint) [ADOPTED từ PRD §11] Form-factor v1: web app Next.js host trên Vercel (user đã xác nhận). Đây là seed stack, không bàn lại.
- (constraint) [ADOPTED từ PRD §11] SAFETY cứng: v1 tuyệt đối KHÔNG có đường code tự gửi lệnh tới sàn. Mọi Đề xuất chỉ hiển thị, người dùng xác nhận thủ công trên sàn.
- (constraint) [ADOPTED từ PRD §10/§4.1] Lõi rule (Tầng 0, Tầng 3, cost hurdle, sizing) phải tất định 100% & tái lập — cùng input → cùng output, để backtest đáng tin. LLM không nằm trên đường quyết định.
- (constraint) [ADOPTED từ PRD Open Q4/§11] Nút thắt kiến trúc trung tâm: Vercel serverless không giữ được tiến trình chạy dài (WebSocket Binance real-time, backtest nặng) → phải tách phần này ra ngoài serverless. Đây là quyết định trụ cột #1 phải giải.
- (direction) Đã tra web (7/2026): Fly/Railway/Render không còn always-on free; Oracle Always Free VM là /bin/zsh-always-on thật; Cloudflare Workers/DO/Containers KHÔNG hợp worker (outbound WS cap 15', DO không hibernate outbound, Containers ngủ+beta+$5). Cloudflare chỉ dùng phụ (R2/DNS/realtime-fanout).
- (decision) [TOPOLOGY v1 — chọn A] Stateless serverless + Postgres + cron poll. Scheduled function (~1') poll Binance REST snapshot → pipeline THUẦN đọc/ghi state trong Postgres → phát Đề xuất. Vercel free (UI+API) + Supabase free (DB+realtime). KHÔNG có tiến trình always-on. Binds: FR-5,FR-1..4,FR-13. Prevents: nhét long-running vào serverless; phân tán state ra RAM.
- (decision) [Rationale A khả thi] Hầu hết tín hiệu v1 poll-được qua REST (funding/OI/L-S/klines); CVD xấp xỉ từ kline taker-volume, tích luỹ trong DB theo phút. Sản phẩm cố ý chống tốc độ (SM-C1) → độ trễ mức phút hợp triết lý. Mất: CVD/orderbook tick live (proxy đủ cho v1).
- (decision) [Backtest] Cùng engine pipeline thuần, chạy như JOB OFFLINE/on-demand (không cần always-on) — bơm data lịch sử thay vì snapshot live. Binds FR-8,FR-9. Đây là hệ quả trực tiếp của pure-core: live & backtest = 2 driver của 1 engine.
- (decision) [PARADIGM] Hexagonal (ports & adapters) quanh decision-core THUẦN; core nội bộ là pipes-and-filters (Tầng0→1→2→3, mỗi tầng pass/veto). Core không import IO. Adapter sau port: ingestion(poll|stream), persistence(Postgres), narrator(LLM), clock, ui. Prevents: IO/thời gian/random rò vào lõi → phá tất định & backtest.
- (decision) [ENGINE LANG] decision-core viết TypeScript, một package dùng chung, consume bởi (1) driver cron/serverless (live) và (2) CLI backtest Node offline. Một engine/một ngôn ngữ/hai driver. Prevents: hai bản cài đặt live vs backtest lệch nhau.
- (decision) [FEEDBACK LOOP — Hybrid] Kết quả lệnh vào Tầng0/Live-drift theo 2 nguồn: (a) Binance read-only API tự dò vị thế/fill/PnL cho daily-loss & drift; (b) user xác nhận 'fill này ứng Đề xuất nào' để gắn đúng win-streak/R. Binds FR-1,FR-10,FR-12,FR-14. Prevents: Tầng0 chạy trên state ma vì không biết kết quả thật.
- (decision) [STATE OWNERSHIP] Behavioral state (win-streak, daily-loss, cooldown, trade-count) là bản ghi Postgres do decision-engine sở hữu; chỉ đổi qua 2 loại event định nghĩa sẵn: market-tick (cron) và trade-outcome (feedback). Một người ghi mỗi loại; không component nào khác mutate. Prevents: hai nơi cùng sửa state kỷ luật → veto sai.
- (decision) [AUDIT LOG] Nhật ký (Đề xuất, tín hiệu kích hoạt, prompt/response LLM, chặn, override) là APPEND-ONLY bất biến trong Postgres — không update/delete. Binds FR-14, NFR auditability. Prevents: sửa lịch sử → mất bằng chứng để user tin/đối chiếu.
- (decision) [DETERMINISM] Trong core cấm wall-clock trực tiếp & random; thời gian là input qua clock-port. Mọi tham số cấu hình (cooldown, ngưỡng, min_rr, risk%) được SNAPSHOT cùng mỗi Đề xuất & mỗi lần backtest → tái lập chính xác. Binds NFR tất định, FR-8/9.
- (decision) [LLM NARRATOR — off-path] LLM chỉ đọc/diễn giải SAU khi rule ra Đề xuất; sau narrator-port; temp thấp; log toàn bộ prompt/response; LLM lỗi → Đề xuất vẫn hiện (ghi chú thiếu lý do). LLM KHÔNG tạo/đổi/bỏ Đề xuất. Default swappable: Claude Haiku (rẻ). Binds FR-7, NFR an-toàn-LLM.
- (decision) [VERCEL ISOLATION] Vercel/Next.js chỉ đọc Postgres + hiển thị + realtime push; KHÔNG chạy pipeline, KHÔNG chạm đường quyết định, KHÔNG có đường gửi lệnh tới sàn. Thỏa ràng buộc SAFETY. Cron runner là adapter riêng (Supabase pg_cron/scheduled function ~1'), không phải UI.
- (version) Next.js 16.2.10 LTS (verified 7/2026). Supabase pg_cron free-tier, minute-level (verified). Node.js 22 LTS backtest runtime. Deno = Supabase Edge Functions runtime.
- (decision) [CRON RUNNER] pg_cron (~1') → pg_net → Supabase Edge Function (Deno) chạy decision-core TS → ghi Postgres. KHÔNG dùng Vercel route cho pipeline (giữ pipeline khỏi Vercel/SAFETY). Lõi TS thuần portable: Deno (edge cron) + Node (backtest CLI).
- (decision) [PIPELINE GATING] Thứ tự bất biến: Tầng0(veto tối cao)→1(regime/hướng)→2(price action/điểm vào)→3(risk/sizing). Bất kỳ tầng chặn → dừng, hệ thống IM LẶNG (không Đề xuất). Cost-hurdle(FR-11) là cổng trong Tầng3; live-drift(FR-10) & override-friction(FR-12) là luật Tầng0. Binds FR-1..4,10,11,12.
- (decision) [INGESTION PORT + degrade] Nguồn dữ liệu sau ingestion-port (v1: Binance REST poll; v2: WS stream — thay adapter, không đụng lõi). Endpoint lỗi/thiếu → suy giảm mềm, KHÔNG phát Đề xuất trên dữ liệu khuyết, ghi log. Binds FR-5,6, NFR bền-dữ-liệu.
- (event) Lint spine: 0 findings. Reviewer lens 1 (tech verified): đạt. Lens 2 (adversarial) tìm 4 lỗ.
- (decision) [AD-12] Suy diễn tín hiệu (CVD tích luỹ, regime, vùng thanh khoản) nằm TRONG core thuần; adapter chỉ giao dữ liệu thô chuẩn hóa → live & backtest tính giống hệt. Vá lỗ: adapter tính CVD khác backtest.
- (decision) [convention] Ranh giới trading-day cấu hình được (mặc định UTC 00:00) cho daily-loss/max-trades/reset chuỗi — một định nghĩa duy nhất. Vá lỗ mốc ngày lệch.
- (decision) [convention] MARKET_SNAPSHOT shape do ingestion adapter sở hữu, một shape chuẩn cho cả live-tick & backtest. Vá lỗ schema snapshot lệch.
- (question) [deferred] Lệnh discretionary có tính vào win-streak/daily-loss? → quyết định sản phẩm+config, mặc định: tính daily-loss/drift, không tính win-streak. Không đổi kiến trúc.
- (event) Input reconciliation: PRD FR-1..14 đều map trong Capability→Architecture. Tone 'im lặng=câu trả lời' giữ ở AD-5. Anti-speed (SM-C1) là rationale topology A. FR-9 walk-forward/holdout/paper = mode của backtest-cli (build detail).
- (event) spine finalized (status: final). Companion SOLUTION-DESIGN.md tạo. Re-lint 0 findings.
