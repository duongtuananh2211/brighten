---
topic: UX Trading DSS Brighten v1 — bề mặt Đề xuất + kỷ luật, web app đọc/xác nhận, solo tool
updated: 2026-07-04T10:16
---

- (event) Create intent, Fast path. Ngôn ngữ VN. Nguồn: PRD + ARCHITECTURE-SPINE + epics.md (Epic 4). Không có tài liệu UX/brand upstream.
- (decision) Form-factor: web app desktop-primary, responsive. UI system: shadcn/ui trên Next.js 16.2 + Tailwind (hợp solo TS dev, khớp stack apps/web). DESIGN.md chỉ đặc tả brand-layer delta trên shadcn defaults.
- (decision) SIGNATURE MOVE: đảo ngữ nghĩa màu — thắng chuỗi/hưng phấn = màu CẢNH BÁO (amber), không phải xanh chiến thắng. Sản phẩm chống dopamine, thắng chuỗi là 'trạng thái nguy hiểm'. Xanh euphoria bị cấm.
- (decision) Im lặng = câu trả lời: trạng thái 'không có edge — chờ' là màn hình hạng nhất, bình thản, KHÔNG phải empty-error. Đây là output, không phải sự vắng mặt.
- (decision) Voice hệ thống: ngôi thứ nhất, trung thực, không hype, không emoji, không dấu chấm than. Vd 'không có edge rõ ràng — chờ', 'đang thắng chuỗi — trạng thái nguy hiểm', 'tôi không còn đáng tin — chờ đã'.
- (decision) Override = ma sát cao có chủ đích: cooldown + gõ tay xác nhận, UI cố tình khó, không phải một cú click.
- (assumption) Cơ chế thông báo Đề xuất (web push/âm thanh/màn hình) — PRD Open Q5 chưa chốt; giả định web push + badge, đánh dấu [ASSUMPTION].
- (assumption) Backtest kết quả xem trên web ở dạng read-only (equity curve, expectancy, R-dist) — engine chạy CLI; giả định có surface xem, [ASSUMPTION].
- (event) Input reconciliation: PRD UJ-1..4 → 4 Key Flows; FR-1/7/10/12/13/14 + SAFETY(AD-10) đều có surface/state. Spines finalized (fast path, reviewer gate + mocks để tùy chọn).
- (event) Rendered key-screen mock .working/now-states.html — màn hình Now qua 5 trạng thái (Đề xuất, im lặng, danger thắng-chuỗi, halt drift, override dialog) + trust rail, light/dark, đúng token DESIGN.md.
